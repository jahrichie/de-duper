== DE-DUPER

This is a simple app that takes the intersection of two arrays and creates a third array of those duplicates. 

I made it to locate duplicates and create a third list between two existing emails lists. 
